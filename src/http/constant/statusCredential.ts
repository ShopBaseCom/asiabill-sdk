import type { StatusCheckCredential } from '../../payment/type';

export const RESULT_VALID: StatusCheckCredential = 'valid';
export const RESULT_INVALID: StatusCheckCredential = 'invalid';
export const RESULT_RESTRICTED: StatusCheckCredential = 'restricted';

